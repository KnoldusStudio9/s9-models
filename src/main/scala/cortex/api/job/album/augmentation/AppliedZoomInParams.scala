package cortex.api.job.album.augmentation

final case class AppliedZoomInParams(
                                      magnification: Float = 0.0f,
                                      resize: Boolean = false
                                    )
