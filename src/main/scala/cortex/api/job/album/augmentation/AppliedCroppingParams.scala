package cortex.api.job.album.augmentation

final case class AppliedCroppingParams(
                                        cropAreaFraction: Float = 0.0f,
                                        resize: Boolean = false
                                      )
